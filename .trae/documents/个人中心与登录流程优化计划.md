## 个人中心与登录流程优化计划

我将为您实现个人信息的真实持久化存储、头像上传功能，并确保应用启动时默认进入登录界面。

### 1. 数据库与后端 (Electron) 增强
- **数据库架构升级**：在 [database.js](file:///c:/Users/jiefeng.lin/Desktop/Project_Auto_Fill_Forms_v3/electron/database.js) 的 `users` 表中增加 `avatar` 字段，用于存储头像文件路径或 Base64。
- **用户服务完善**：
    - 更新 [UserService.js](file:///c:/Users/jiefeng.lin/Desktop/Project_Auto_Fill_Forms_v3/electron/services/UserService.js) 中的 `login` 方法，返回用户的完整信息（包含邮箱、手机号、头像、注册时间）。
    - 新增 `updateUserProfile` 方法，支持修改昵称、邮箱、手机号等信息。
- **文件存储系统**：在主进程中实现头像上传逻辑，将用户上传的图片保存到本地 `userData/avatars` 目录中，确保头像持久化。

### 2. 前端状态与个人中心优化
- **Store 同步**：更新 [account.js](file:///c:/Users/jiefeng.lin/Desktop/Project_Auto_Fill_Forms_v3/src/stores/account.js)，使其能够管理并持久化完整的用户信息。
- **个人中心对接**：重构 [Profile.vue](file:///c:/Users/jiefeng.lin/Desktop/Project_Auto_Fill_Forms_v3/src/views/profile/Profile.vue)：
    - 将界面上的“用户名”、“邮箱”、“手机号”等字段直接绑定到数据库返回的真实数据。
    - 实现真实的“上传头像”功能，调用 Electron 接口保存文件。
    - 将“注册时间”从模拟数据改为显示真实的 `created_at` 字段。

### 3. 登录流程强制化
- **启动强制登录**：修改路由守卫或 Store 初始化逻辑，确保每次 `npm run dev` 启动应用时，除非有有效的会话，否则必须先经过登录界面。
- **路由守卫增强**：优化 [router/index.js](file:///c:/Users/jiefeng.lin/Desktop/Project_Auto_Fill_Forms_v3/src/router/index.js)，严防未登录状态下通过 URL 直接访问主页面。

---

**是否现在开始执行这些改进？点击确认后我将立即动手！🚀**